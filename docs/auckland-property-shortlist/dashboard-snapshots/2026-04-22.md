# 📂 看板快照 · 2026-04-22

> 这是项目看板在 2026-04-22 的状态归档。最新版请看 [项目看板](../dashboard/)。
>
> 本期为 Dashboard **首版初始化**, 无旧版归档内容。

---

（以下是本期看板内容)

---

# 奥克兰在售房产搜索工具 · 项目看板

> **最近更新**: 2026-04-22
>
> _自用项目 · 无外部客户 · 内部进度追踪_

---

## 📍 当前状态

**整体进度**: 80%

🚀 **下一节点 · 2026-04-23**: PR #8 合并 + Deploy to PROD 回归验证 (PROD Live, 验证 deploy.sh 顺序修复是否彻底解决 venv 被 git clean 误删的连锁问题)

核心交付已经完成 — 桌面版 + 云端 Web 平台都在运行, 180 天历史数据 5,306 条房源加载完毕。剩余 20% 是增量功能 (lifecycle 管理 / HTTPS / 私信) 与长期运维迭代。

---

## ⚠️ 本周待处理

**PR #8 等合并**

`deploy.sh` 顺序调整 — `git clean` 挪到 `reset --hard` 之后, 防止 clean 在读旧 `.gitignore` 时把 `venv/bin/` 当未追踪文件删掉。合并后需要跑一次 Deploy to PROD 做回归, 确认 venv 不再被误删。

---

## 📆 最近 1.5 周进展 (4/11 - 4/22)

### ⭐ 云端 Web 平台已上线 Oracle Cloud (本期重点)

- **以前**: 仅 Windows 桌面单机版 (PyInstaller onedir + Ed25519 离线授权), 使用场景受限于发行方签发 license
- **今后**: 云端 Web 平台已上 Oracle VM `aps-web` (161.33.80.117)。技术栈: FastAPI + Jinja2 + Tabulator 6.x, SQLite (WAL) + SQLAlchemy 2.0 ORM, systemd 管理三进程 (web / worker / pipeline), cron 每小时 NZ 0:00+6:00-23:00 自动抓取, GitHub Actions CI/CD (dev→PR→main→Deploy to PROD)
- **好处**: 里程碑级 — 从单机工具跨到多用户云产品, 180 天历史数据 5,306 条房源完整加载, 后续可挂 HTTPS/域名/私信做 SaaS 形态

### Re-enrich 机制全流程实施

- **以前**: GIS 数据更新后需要手动 SSH 重跑 pipeline, 无独立 CLI, 无 cron 托管
- **今后**: reenrich.py CLI + service 化 + cron try/finally lifecycle + sudoers 自动安装 + PROD smoke 17m44s 端到端成功 (2946 accepted + 2332 rejected)
- **好处**: GIS 数据或评分算法升级后可一条命令触发全量重算, 不再手动处理

### PROD 缩略图恢复 + schools 分隔符修复

- **以前**: PROD 2947 行全无缩略图 (requirements.txt 漏 matplotlib) + 学校列 `\n` 分隔不渲染挤成一团
- **今后**: PR #6/#7/#8 连锁修完 — 加 matplotlib + APS.fmtSchoolList(\n→; ) + .gitignore 加 venv/ + deploy.sh 顺序调整。Reenrich 跑完 has_png=2947/2947, Playwright 22/22 验证通过
- **好处**: 前端体验恢复完整, 同时暴露并修复了 deploy.sh 的 git clean 误删 venv 连锁 bug

### Deploy.sh 加固 (PR #5)

- **以前**: checkout + pull 组合遇到 VM 本地漂移 (文件 mode 变化, 未追踪的 .gitignore) 会报错阻塞部署
- **今后**: reset --hard 取代 checkout+pull (VM 漂移自动抹平), sudoers 自动安装 (_systemctl 检查真实 returncode)
- **好处**: CI/CD 流水线更鲁棒, VM 人工运维残留可自愈

---

## 🗺 未来计划

🎯 **2026-04-23 · 近期**
PR #8 合并 + Deploy to PROD 回归验证
验证 deploy.sh 顺序修复是否彻底解决 venv 被误删问题 (PROD Live)

🔧 **2026-04 月底**
Listing lifecycle 管理
加 is_active flag + 下架房源检测, 避免 PROD 保留已从 Trade Me 下架的房源

💰 **2026-05 中**
HTTPS + 自定义域名
当前仅 IP 直连 (161.33.80.117:8080), 切 HTTPS + 域名提升可用性

🚀 **2026-05 下**
Private messaging + 外部集成探索
Admin ↔ User 站内私信 + Teams/WeChat 集成探索
